package api

// defaultDockerEndpoint is default docker endpoint
const defaultDockerEndpoint = "unix:///var/run/docker.sock"

// defaultAccessTokenLength is default access token lenge
const defaultAccessTokenLength = 100

// defaultIPRangePrefix is default prefix ip address in docker network (x then dynamic number)
const defaultIPRangePrefix = "192.168.x.0"
